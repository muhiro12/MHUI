import SwiftUI

enum MHColorAsset {
    static let background = ColorResource(
        name: "MHBackground",
        bundle: .module
    )

    static let surface = ColorResource(
        name: "MHSurface",
        bundle: .module
    )

    static let surfaceElevated = ColorResource(
        name: "MHSurfaceElevated",
        bundle: .module
    )

    static let surfaceMuted = ColorResource(
        name: "MHSurfaceMuted",
        bundle: .module
    )

    static let border = ColorResource(
        name: "MHBorder",
        bundle: .module
    )

    static let primaryText = ColorResource(
        name: "MHPrimaryText",
        bundle: .module
    )

    static let secondaryText = ColorResource(
        name: "MHSecondaryText",
        bundle: .module
    )

    static let tertiaryText = ColorResource(
        name: "MHTertiaryText",
        bundle: .module
    )

    static let accent = ColorResource(
        name: "MHAccent",
        bundle: .module
    )

    static let onAccent = ColorResource(
        name: "MHOnAccent",
        bundle: .module
    )
}
